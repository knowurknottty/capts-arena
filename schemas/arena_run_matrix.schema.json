{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "file:///schemas/arena_run_matrix.schema.json",
  "title": "ArenaRunMatrix",
  "description": "Defines a structured Arena comparison run using real candidate score reports.",
  "type": "object",
  "required": [
    "arena_run_id",
    "benchmark_suite",
    "scoring_version",
    "baseline_candidate",
    "challenger_candidates"
  ],
  "properties": {
    "arena_run_id": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" },
    "benchmark_suite": { "type": "string" },
    "case_filter": { "type": "string", "default": "all" },
    "scoring_version": { "type": "string" },
    "comparison_mode": {
      "type": "string",
      "enum": ["pairwise", "all_vs_all", "king_defense"]
    },
    "baseline_candidate": { "type": "string" },
    "challenger_candidates": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1
    },
    "strict_case_match": { "type": "boolean", "default": true },
    "candidate_reports": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },
    "reports_dir": { "type": "string" },
    "controlled_variables": {
      "type": "object",
      "properties": {
        "temperature": { "type": ["number", "null"] },
        "top_p": { "type": ["number", "null"] },
        "tool_policy": { "type": "string" },
        "memory_snapshot": { "type": "string" },
        "benchmark_cases": { "type": "string" },
        "scorer": { "type": "string" }
      }
    },
    "outputs": {
      "type": "object",
      "properties": {
        "run_root": { "type": "string" },
        "pairwise_reports_dir": { "type": "string" },
        "kingboard_path": { "type": "string" },
        "failure_museum_dir": { "type": "string" },
        "promotion_path": { "type": "string" }
      }
    }
  }
}
